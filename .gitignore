# Build (ESP-IDF / PlatformIO / Arduino)
build/
.pio/
managed_components/
sdkconfig.old

# Binaria
*.bin
*.elf
*.map
*.o
# wyjątek: prebuilt firmware dla web flashera (serwowany przez raw.githubusercontent)
!firmware/sensmos-esp32.bin

# Sekrety (gdyby kiedyś doszły)
secrets.h
*.pem
*.key

# IDE / OS
.vscode/
*.log
.DS_Store
